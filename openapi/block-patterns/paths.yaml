paths:
  /wp/v2/block-patterns/patterns:
    get:
      tags:
      - block patterns
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './schemas.yaml#/schemas/BlockPatternResponse'
  /wp/v2/block-patterns/categories:
    get:
      tags:
      - block patterns
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './schemas.yaml#/schemas/BlockPatternCategoryResponse'
